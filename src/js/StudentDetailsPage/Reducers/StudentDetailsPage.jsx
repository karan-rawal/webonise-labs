const initialState = {
  // TODO
};

const StudentDetailsPageReducer = (state = initialState) => {
  let tempState = {};
  Object.assign(tempState, state);
  return state;
};

export default StudentDetailsPageReducer;
